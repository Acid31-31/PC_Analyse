<Window x:Class="PCAnalyse.Verbindung.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PC Analyse Verbindung" Height="520" Width="720"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource AppBackground}"
        Foreground="{DynamicResource TextPrimary}"
        FontFamily="Segoe UI"
        Closed="Window_Closed">

    <Window.Resources>
        <Style x:Key="TitleBarBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1A2836"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TitleBarCloseBtn" TargetType="Button" BasedOn="{StaticResource TitleBarBtn}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C62828"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="HeaderUtilityBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <StackPanel Orientation="Horizontal">
                            <ContentPresenter/>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="96"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="36"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource HeaderBackground}"
                BorderBrush="{DynamicResource GroupBorder}" BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid Margin="24,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Width="68" Height="68" CornerRadius="12"
                        Background="{DynamicResource AccentSoftBrush}"
                        BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1"
                        VerticalAlignment="Center" Margin="0,0,16,0">
                    <TextBlock Text="PC" FontSize="22" FontWeight="Bold"
                               Foreground="{DynamicResource AccentBrush}"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Verbindung" FontSize="22" FontWeight="Bold"
                               Foreground="{DynamicResource HeaderForeground}"/>
                    <TextBlock Text="Zweiter PC – ohne Suche, ohne Passwort"
                               FontSize="13" Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource HeaderUtilityBtn}" Click="CheckForUpdates_Click" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⟳" Margin="0,0,6,0"/>
                            <TextBlock Text="Update"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource TitleBarBtn}" Content="—" Click="Minimize_Click"/>
                    <Button Style="{StaticResource TitleBarCloseBtn}" Content="✕" Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="24" Padding="28" CornerRadius="8"
                Background="{DynamicResource CardBackground}"
                BorderBrush="{DynamicResource GroupBorder}" BorderThickness="1">
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="MachineText" FontSize="20" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="Dieses Programm einfach starten und offen lassen."
                           Foreground="{DynamicResource TextSecondary}" FontSize="14"
                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                <TextBlock Text="Der andere PC findet diesen Rechner automatisch. Eine Maus steuert beide: Cursor über den linken Rand schieben."
                           Foreground="{DynamicResource TextSecondary}" FontSize="14"
                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="0,8,0,24"/>
                <ProgressBar x:Name="WaitBar" IsIndeterminate="True" Height="6" Margin="40,0"/>
                <TextBlock x:Name="WaitText" Text="Wartet auf Verbindung …"
                           Foreground="{DynamicResource AccentBrush}" FontSize="16" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,20,0,0"/>
                <TextBlock x:Name="MouseShareText" Foreground="{DynamicResource TextSecondary}" FontSize="13"
                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="0,12,0,0"/>
                <Button Content="Maus auf den anderen PC" Click="SwitchMouse_Click"
                        Margin="0,18,0,0" Padding="18,10" HorizontalAlignment="Center"
                        Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource TextOnAccent}"
                        BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource FooterBackground}"
                BorderBrush="{DynamicResource GroupBorder}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                <TextBlock Text="✓" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" Margin="0,0,8,0"/>
                <TextBlock x:Name="StatusText" Text="Bereit" FontSize="13"
                           Foreground="{DynamicResource TextSecondary}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
