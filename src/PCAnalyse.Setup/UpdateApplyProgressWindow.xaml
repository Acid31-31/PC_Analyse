<Window x:Class="PCAnalyse.Setup.UpdateApplyProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update wird installiert"
        Height="280" Width="480"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackground}"
        Foreground="{DynamicResource TextPrimary}"
        FontFamily="Segoe UI"
        Topmost="True">

    <Border BorderBrush="{DynamicResource GroupBorder}" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Update wird installiert…" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
            <ProgressBar x:Name="InstallProgressBar" Grid.Row="1" Height="10" Minimum="0" Maximum="100"
                         Foreground="{DynamicResource ProgressFill}" Background="{DynamicResource ProgressTrack}"/>
            <Grid Grid.Row="2" Margin="0,10,0,0">
                <TextBlock x:Name="StatusTextBlock" FontSize="12" Foreground="{DynamicResource TextSecondary}"
                           TextWrapping="Wrap"/>
                <TextBlock x:Name="PercentTextBlock" HorizontalAlignment="Right" FontSize="12" FontWeight="SemiBold"
                           Foreground="{DynamicResource ProgressFill}"/>
            </Grid>
            <Button x:Name="CancelButton" Grid.Row="3" Content="Abbrechen – später" MinWidth="160" Height="34"
                    HorizontalAlignment="Right" Margin="0,16,0,0" Click="CancelButton_Click"/>
        </Grid>
    </Border>
</Window>
